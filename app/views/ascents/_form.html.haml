=form_for @ascent do |f|

  =f.label :ascent_id, class: 'form-group'
  =f.text_field :ascent_id, class: 'form-control'

  =f.label :name, class: 'form-group'
  =f.text_field :name, class: 'form-control'

  =f.label :comment, class: 'form-group'
  =f.text_field :comment, class: 'form-control'

  =f.label :date, class: 'form-group'
  =f.date_field :date, class: 'form-control'

  =f.label :style, class: 'form-group'
  =f.text_field :longitude, class: 'form-control'
  %br/
  .form-group
    =f.submit class: 'btn btn-success'






%p
  = link_to 'Back', crag_sector_climbing_route_ascents_path
= render 'errors', model: @ascent
= form_for @ascent do |f|
  %p
    = f.label :style
    %br/
    = f.collection_select(:ascent_style_id, @ascents, :id, :name, {:prompt => "Select sector..."}, {:selected => :prompt})
  %p
    = f.label :comment
    %br/
    = f.text_field :text
  %p
    = f.label :date
    %br/
    = f.text_area :datetime
  %p
  / %p= 

  .actions
    = f.submit 'Save'

